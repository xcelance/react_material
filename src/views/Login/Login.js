import React from 'react';
import { CardHeader, Divider } from '@material-ui/core';
import './style.scss';

const Login = () => {

    return (
        <div>
            Login here.
        </div>
    );
};

export default Login;
